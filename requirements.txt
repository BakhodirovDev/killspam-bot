# Pinned to the versions the resolver settled on, so builds are fast + reproducible.
# google-genai is held at 1.46.0: 2.x conflicts with aiogram 3.13's pydantic<2.9 /
# httpx stack and made pip backtrack through ~40 versions. Dependabot bumps these.
aiogram==3.13.0
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
python-dotenv==1.0.1
google-genai==1.46.0
nudenet==3.4.2
cryptography==49.0.0
aiohttp==3.10.11
